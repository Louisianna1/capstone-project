import { Fragment } from 'react';

const LoginPage = () =>  {
    return (
        <div className="sectionContainer">
            <h1>Login</h1>
        </div>
      );
    }

export default LoginPage;
